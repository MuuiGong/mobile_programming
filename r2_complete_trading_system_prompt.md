# R² 모의투자 시스템 완전 구체화 프롬프트 (AI용)

## 📌 프롬프트 시작

다음은 **R² 모의투자 앱**의 **거래 시스템 구현 가이드**입니다.
모든 사항을 정확히 따라 구현해주세요.

---

## 🎯 핵심 시스템 설계

### 1. 거래 유형 (두 가지 모드)

#### 1.1 현물 거래 (Spot Trading)
```
특징:
  - 레버리지: 없음 (1x)
  - 자산: 실제 보유
  - 위험: 낮음
  - 예시: $100으로 0.052 BTC 매수

계산식:
  거래 수량 = 거래 자금 / 진입 가격
           = $100 / $95,836
           = 0.001044 BTC (약)
```

#### 1.2 선물 거래 (Futures Trading) - 새로 추가
```
특징:
  - 레버리지: 1x ~ 20x (사용자 선택)
  - 자산: 유도파생상품
  - 위험: 매우 높음
  - 예시: $100으로 1x ~ 20x 레버리지 거래

계산식 (5x 레버리지 예):
  실제 자금: $100
  거래 규모: $100 × 5 = $500
  거래 수량: $500 / $95,836 = 0.00522 BTC
  
  IF TP 도달 ($97,752):
    이익 = ($97,752 - $95,836) × 0.00522 × 5 = +$49.95 (5배)
    
  IF SL 도달 ($93,919):
    손실 = ($95,836 - $93,919) × 0.00522 × 5 = -$49.95 (5배)
```

---

## 📊 초기 설정 (First Launch)

### 2. 사용자 설정 화면

```
┌─────────────────────────────────┐
│     R² 모의투자 설정            │
├─────────────────────────────────┤
│ 1. 초기 자본                    │
│   입력: [        $10,000      ] │
│                                 │
│ 2. 거래 모드 선택              │
│   ○ 현물 거래 (1x)            │
│   ◉ 선물 거래 (레버리지)       │
│                                 │
│   레버리지 기본값: [1x v]       │
│   (1x, 2x, 3x, 5x, 10x, 20x)  │
│                                 │
│ 3. 1회 거래 위험 관리          │
│   □ 고정액 방식                │
│     위험 자금: [  $100  ]      │
│                                 │
│   ☑ 비율 방식                 │
│     비율: [  2  ]%             │
│                                 │
│ 4. 포지션 관리                  │
│   최대 동시 포지션: [3]개       │
│   1회 최대 손실: [5]%           │
│   일일 최대 손실: [10]%         │
│   포지션 최대 지속: [무제한 v]  │
│                                 │
│ 5. 거래 기호 선택              │
│   [BTCUSDT] [ETHUSDT] [+추가] │
│                                 │
│ 6. 차트 설정                    │
│   타임프레임: [1H v]           │
│   차트 유형: [Candlestick v]   │
│                                 │
│      [설정 완료] [취소]        │
└─────────────────────────────────┘
```

---

## 💰 거래 계산 엔진

### 3. 거래 크기 계산

#### 3.1 현물 거래 (레버리지 없음)
```pseudocode
FUNCTION calculateSpotTradeSize(riskAmount, entryPrice):
    tradeSize = riskAmount / entryPrice
    RETURN tradeSize

EXAMPLE:
    riskAmount = $100
    entryPrice = $95,836
    tradeSize = 100 / 95,836 = 0.001044 BTC
```

#### 3.2 선물 거래 (레버리지 포함)
```pseudocode
FUNCTION calculateFuturesTradeSize(riskAmount, leverage, entryPrice):
    totalFunds = riskAmount * leverage
    tradeSize = totalFunds / entryPrice
    RETURN tradeSize

EXAMPLE (5x 레버리지):
    riskAmount = $100
    leverage = 5
    totalFunds = $100 * 5 = $500
    entryPrice = $95,836
    tradeSize = 500 / 95,836 = 0.005220 BTC
```

#### 3.3 손실/수익 계산
```pseudocode
FUNCTION calculatePnL(tradeSize, entryPrice, exitPrice, leverage):
    pnl = (exitPrice - entryPrice) * tradeSize * leverage
    pnlPercent = (pnl / (tradeSize * entryPrice)) * 100
    RETURN {pnl, pnlPercent}

EXAMPLE (5x 선물):
    tradeSize = 0.005220 BTC
    entryPrice = $95,836
    exitPrice = $97,752 (TP 도달)
    leverage = 5
    pnl = (97,752 - 95,836) * 0.005220 * 5 = $49.95
    pnlPercent = (49.95 / (0.005220 * 95,836)) * 100 = +100%
    
EXAMPLE (5x 선물, 손실):
    exitPrice = $93,919 (SL 도달)
    pnl = (93,919 - 95,836) * 0.005220 * 5 = -49.95
    pnlPercent = -100%
```

---

## 📈 실시간 거래 모니터링

### 4. 포지션 상태 추적

#### 4.1 실시간 포지션 카드
```
┌─────────────────────────────────┐
│  BTCUSDT | 1H | 5x | +0:45:30  │
├─────────────────────────────────┤
│ Entry: $95,836.00               │
│ TP: $97,752.72                  │
│ SL: $93,919.28                  │
│ Leverage: 5x                    │
├─────────────────────────────────┤
│ 현재가: $96,500.00              │
│ 현재 P&L: +$33.30 (+33.3%)     │
│ R:R: 1.24:1                     │
│ 수익도: 42% (TP까지 남은 거리)  │
│ 손실도: 58% (SL까지 남은 거리)  │
├─────────────────────────────────┤
│ 거래 크기: 0.026100 BTC         │
│ 위험 자금: $100                 │
│ 포지션 가치: $2,522.99          │
│ 마진 사용: $504.60 (20%)        │
│ 가용 마진: $2,018.39            │
├─────────────────────────────────┤
│ [수동 종료] [편집] [상세보기]   │
└─────────────────────────────────┘
```

#### 4.2 실시간 계산 (매초 업데이트)
```pseudocode
FUNCTION updateLivePosition(currentPrice, position):
    pnl = (currentPrice - position.entryPrice) 
          * position.tradeSize 
          * position.leverage
    
    pnlPercent = (pnl / position.initialRisk) * 100
    
    remainingToTP = position.tp - currentPrice
    remainingToSL = currentPrice - position.sl
    
    profitRatio = remainingToTP / (position.tp - position.entry)
    lossRatio = remainingToSL / (position.entry - position.sl)
    
    RETURN {
        currentPrice,
        pnl,
        pnlPercent,
        remainingToTP,
        remainingToSL,
        profitRatio,
        lossRatio
    }

UPDATE FREQUENCY: 1초마다 (또는 가격 변경 시)
```

---

## 🎯 자동 매도 시스템

### 5. 자동 종료 조건

#### 5.1 TP 도달 (익절)
```pseudocode
IF currentPrice >= tp:
    closeTrade(position, exitPrice=tp, reason="TP_HIT")
    
    CALCULATE:
        maxProfit = (tp - entry) * tradeSize * leverage
        
    NOTIFY:
        title = "익절! TP 도달"
        message = f"Entry: ${entry} | TP: ${tp} | 수익: +${maxProfit}"
        sound = "success_bell"
        
    RECORD:
        tradeId = UUID
        exitReason = "TP_HIT"
        exitPrice = tp
        exitTime = NOW()
        duration = exitTime - entryTime
```

#### 5.2 SL 도달 (손절)
```pseudocode
IF currentPrice <= sl:
    closeTrade(position, exitPrice=sl, reason="SL_HIT")
    
    CALCULATE:
        maxLoss = (entry - sl) * tradeSize * leverage
        
    NOTIFY:
        title = "손절! SL 도달"
        message = f"Entry: ${entry} | SL: ${sl} | 손실: -${maxLoss}"
        sound = "alert_alarm"
        color = RED
        
    RECORD:
        exitReason = "SL_HIT"
        exitPrice = sl
```

#### 5.3 마진콜 (선물 거래만)
```pseudocode
IF leverage > 1:
    usedMargin = (tradeSize * entryPrice) / leverage
    totalMargin = usedMargin + pnl
    marginRatio = totalMargin / usedMargin
    
    IF marginRatio <= 0.5:  // 마진 50% 이하
        ALERT: "마진콜! 포지션 강제 종료 예정"
        
    IF marginRatio <= 0:    // 마진 부족
        closeTrade(position, exitPrice=currentPrice, reason="MARGIN_CALL")
        NOTIFY: "마진콜로 포지션이 강제 종료되었습니다"
```

#### 5.4 타임아웃 (선택)
```pseudocode
IF userSetting.maxDuration != "UNLIMITED":
    duration = NOW() - entryTime
    
    IF duration >= userSetting.maxDuration:
        closeTrade(position, exitPrice=currentPrice, reason="TIMEOUT")
        NOTIFY: f"타임아웃! {duration} 후 포지션 종료"
```

#### 5.5 일일 손실 한도
```pseudocode
dailyLoss = SUM(losses TODAY)

IF dailyLoss > userSetting.dailyLossLimit:
    DISABLE_ENTRY_BUTTON()
    NOTIFY: "일일 손실 한도에 도달했습니다. 내일 거래할 수 있습니다."
    
RESET_TIME: 00:00 UTC (또는 사용자 설정 타임존)
```

---

## 📊 위험 관리 (Risk Management)

### 6. 거래 전 검증

#### 6.1 거래 생성 전 체크리스트
```pseudocode
FUNCTION validateTrade(entry, tp, sl, riskAmount, leverage):
    errors = []
    
    // 기본 검증
    IF tp <= entry:
        errors.append("TP가 Entry보다 낮습니다")
    
    IF sl >= entry:
        errors.append("SL이 Entry보다 높습니다")
    
    // 거리 검증
    tpDistance = (tp - entry) / entry * 100
    slDistance = (entry - sl) / entry * 100
    
    IF tpDistance < 0.1:  // TP가 0.1% 미만
        errors.append("TP 거리가 너무 작습니다 (최소 0.1%)")
    
    IF slDistance < 0.1:  // SL이 0.1% 미만
        errors.append("SL 거리가 너무 작습니다 (최소 0.1%)")
    
    // 위험도 검증
    maxLoss = (entry - sl) * tradeSize * leverage
    
    IF maxLoss > userSetting.maxLossPerTrade:
        errors.append(f"1회 거래 손실 한도 초과 (한도: ${userSetting.maxLossPerTrade})")
    
    // 포지션 수 검증
    IF activePositions.count >= userSetting.maxPositions:
        errors.append(f"최대 포지션에 도달했습니다 (한도: {userSetting.maxPositions})")
    
    // 일일 손실 검증
    IF dailyLoss + maxLoss > userSetting.dailyLossLimit:
        errors.append(f"일일 손실 한도 초과 예상")
    
    IF errors.isEmpty():
        RETURN {success: true}
    ELSE:
        RETURN {success: false, errors: errors}
```

#### 6.2 실시간 경고
```
경고 레벨 1 (노란색): R:R < 1:1
  "위험도가 높습니다. R:R을 확인하세요"
  
경고 레벨 2 (주황색): 1회 손실 > 초기 잔고의 5%
  "위험도가 매우 높습니다"
  
경고 레벨 3 (빨강색): 선물 거래, 레버리지 > 10x
  "매우 위험합니다. 신중하게 진행하세요"
```

---

## 📋 거래 기록 저장

### 7. 거래 데이터 구조

```json
{
  "tradeId": "trade_20251116_001",
  "symbol": "BINANCE:BTCUSDT",
  "timeframe": "1H",
  "tradeType": "FUTURES",
  "leverage": 5,
  "entryTime": "2025-11-16T18:00:00Z",
  "entryPrice": 95836.00,
  "entrySignal": "MANUAL",
  "tradeSize": 0.005220,
  "tp": 97752.72,
  "sl": 93919.28,
  "riskAmount": 100.00,
  "exitTime": "2025-11-16T18:45:30Z",
  "exitPrice": 97752.72,
  "exitReason": "TP_HIT",
  "pnl": 49.95,
  "pnlPercent": 49.95,
  "duration": "00:45:30",
  "maxDrawdown": -15.50,
  "rrRatio": 1.24,
  "status": "CLOSED"
}
```

#### 7.1 거래 기록 쿼리
```pseudocode
// 필터링
trades = queryTrades(
    dateRange: [startDate, endDate],
    symbols: [BTCUSDT, ETHUSDT],
    tradeTypes: [SPOT, FUTURES],
    exitReasons: [TP_HIT, SL_HIT, MANUAL],
    minPnl: -500,
    maxPnl: 1000
)

// 통계 계산
stats = {
    totalTrades: COUNT(trades),
    winTrades: COUNT(trades WHERE pnl > 0),
    lossTrades: COUNT(trades WHERE pnl < 0),
    winRate: winTrades / totalTrades * 100,
    totalPnL: SUM(trades.pnl),
    avgPnL: AVG(trades.pnl),
    avgRR: AVG(trades.rrRatio),
    maxWin: MAX(trades.pnl),
    maxLoss: MIN(trades.pnl),
    sharpeRatio: CALCULATE_SHARPE(trades),
    maxDrawdown: CALCULATE_MDD(trades)
}
```

---

## 🎨 UI/UX 화면 구조

### 8. 주요 화면들

#### 8.1 대시보드 (메인)
```
┌─────────────────────────────────┐
│       R² 모의투자 대시보드       │
├─────────────────────────────────┤
│ 📊 계정 정보                    │
│   잔고: $10,000.00              │
│   순자산: $10,250.50            │
│   변화: +$250.50 (+2.5%) ✓     │
│                                 │
│ ⚠️  위험 지표                   │
│   Risk Score: 75/100            │
│   일일 손실: -$245 / -$1,000   │
│   사용 마진: $504.60 (활성)    │
│                                 │
│ 📈 오늘의 성과                  │
│   총 거래: 5회                  │
│   승리: 3회 (60%)               │
│   패배: 2회 (40%)               │
│   총 수익: +$250.50             │
│                                 │
│ 🔴 활성 포지션: 2개            │
│  [위치션 1] BTCUSDT | 5x       │
│  [위치션 2] ETHUSDT | 1x       │
│                                 │
│ [+ 새 거래] [기록 보기]        │
└─────────────────────────────────┘
```

#### 8.2 거래 화면 (차트 + 제어)
```
[차트 영역]
─────────────────────────────────

[라이브 포지션 모니터]
Position 1: +33.3% | Position 2: -5.1%

[거래 컨트롤]
┌─────────────────────────────────┐
│ Entry: [________________] $95836 │
│ TP:    [________________] $97752 │
│ SL:    [________________] $93919 │
│ Lev:   [1x________] (1~20x)     │
│─────────────────────────────────│
│ 위험: $100 | 수익: +$1,916      │
│ R:R: 1.24:1 | 거래 크기: 0.052 │
│─────────────────────────────────│
│    [진입 거래] [초기화]         │
└─────────────────────────────────┘
```

#### 8.3 거래 기록 화면
```
┌─────────────────────────────────┐
│        거래 기록                │
├─────────────────────────────────┤
│ 필터: [오늘▼] [전체▼] [검색]   │
│─────────────────────────────────│
│ # 심볼  입장시간  수익/손실  상태 │
├─────────────────────────────────┤
│ 1 BTC  18:00    +$49.95  ✓TP   │
│ 2 ETH  17:30    -$23.50  ✗SL   │
│ 3 BTC  17:00    +$100.00 ✓TP   │
│ 4 ETH  16:45    -$75.20  ✗SL   │
│ 5 BTC  16:30    +$25.30  ✓TP   │
│─────────────────────────────────│
│ 통계:                           │
│ 총 거래: 5 | 승률: 60%          │
│ 총 수익: +$250.55               │
└─────────────────────────────────┘
```

---

## 🔧 개발 체크리스트

```
[ ] 1. 거래 설정 저장 (SharedPreferences)
[ ] 2. 거래 크기 계산 엔진
[ ] 3. 실시간 가격 업데이트 (CoinGecko API)
[ ] 4. 자동 종료 시스템 (TP/SL/마진콜)
[ ] 5. 거래 검증 시스템
[ ] 6. 거래 기록 저장 (Room DB)
[ ] 7. 알림 시스템 (NotificationManager)
[ ] 8. 포지션 모니터링 UI
[ ] 9. 거래 기록 조회 및 필터
[ ] 10. 통계 계산 (Sharpe, MDD)
[ ] 11. 레버리지 시뮬레이션
[ ] 12. 마진콜 시뮬레이션
[ ] 13. 단위 테스트 (JUnit)
[ ] 14. UI 테스트 (Espresso)
```

---

## 📝 레버리지 도입 가이드

### 레버리지는 "좋은가"?

#### ✅ 장점
- 적은 자본으로 큰 이익 가능
- 단기 거래에 유용
- 리스크 트레이닝에 효과적

#### ❌ 단점
- 손실도 배가됨
- 마진콜 위험
- 심리적 압박 증가

### 추천사항
```
초기자에게: 1x ~ 2x (현물 또는 저레버리지)
중급자에게: 2x ~ 5x (충분한 자금 관리 후)
고급자에게: 5x ~ 20x (위험 관리 능력 필수)

모의투자이므로: 모든 레버리지 제공 권장
  → 사용자가 학습 가능
  → 실제 거래 전 경험
```

---

## 🚀 구현 시작

이제 이 가이드대로 R² 모의투자 시스템을 구현하세요.

모든 계산식, 검증, UI/UX, 데이터베이스 구조가 명시되어 있습니다.

**구현 시작!** 💪

